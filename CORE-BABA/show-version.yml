---
- name: Show CORE BABA version
  hosts: baba
  gather_facts: false

  tasks:
    - name: Run show version
      ios_command:
        commands:
          - show version
      register: output

    - name: Display output
      debug:
        var: output.stdout_lines
